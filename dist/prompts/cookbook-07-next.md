Continue Signal Garden from its actual current state. Read docs/progress.md, the acceptance checks and the relevant code; verify the previous checkpoint before trusting the note. Implement the next incomplete playable milestone: win loop, then drone/health/time loss, then visual polish, then release readiness. For this run, finish one milestone and its affected checks. Preserve the agreed rules, including loss-before-activation, single collection, cooldown timing and complete restart. Use placeholder assets until mechanics pass. Record the commands, observed results, remaining failures and next step. Review and commit only this milestone's intended changes after checks pass. Leave publication to the release step.
